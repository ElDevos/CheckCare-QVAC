Benchmark logic lives in `apps/edge/src/qvac/benchmark.ts` (run via `npm run benchmark`).
Generated benchmark reports are written to `../reports/benchmark-*.json`, alongside the evaluation
reports, so results from `npm run evaluate` and `npm run benchmark` stay in one place.
